// Question Five:
// Write a function called getLocationsSortedBySize that takes in an array of objects that look like the array below,
// and returns an array of the strings corresponding to the value of the location property
// The output should be in the same order as the input

/**

* Returns an array of country names sorted in descending order by population

* @param {object[]} arr - The input array.  Objects will be in the form: { country: "foo", population: 10 }
* @returns {string[]} - Returns an array of country names, sorted by their population in descending order

*/

function getCountriesSortedByPopulation() {

}

module.exports = getCountriesSortedByPopulation
